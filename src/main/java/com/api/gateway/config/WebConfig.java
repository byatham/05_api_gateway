/*
 * package com.api.gateway.config;
 * 
 * import org.springframework.context.annotation.Configuration; import
 * org.springframework.web.servlet.config.annotation.CorsRegistry; import
 * org.springframework.web.servlet.config.annotation.WebMvcConfigurer;
 * 
 * @Configuration public class WebConfig implements WebMvcConfigurer {
 * 
 * 
 * @Override public void
 * addCorsMappings(org.springframework.web.reactive.config.CorsRegistry
 * registry) { registry.addMapping("/**") .allowedOrigins("*") // Adjust this to
 * specify allowed origins .allowedMethods("*") // Adjust this to specify
 * allowed methods .allowedHeaders("*"); // Adjust this to specify allowed
 * headers } }
 */